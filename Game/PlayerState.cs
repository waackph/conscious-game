namespace conscious
{
    /// <summary>Enum <c>PlayerState</c> contains states the player can be in, changing the animation.
    /// </summary>
    ///
    public enum PlayerState
    {
        Idle,
        Walk,
        Sleep,
        None
    }
}